/**
 * Moderation.jsx — Admin + Host shared moderation page.
 */

import "../stylesheets/Moderation.css";

export default function Moderation() {
  return (
    <main>
      <h1>Moderation</h1>
    </main>
  );
}
